# Docker-specific overrides
# These settings override env.example for Docker container execution
# All other settings are inherited from env.example

# Prometheus / Mimir connection
PROMETHEUS_URL=http://host.docker.internal:9090

# ClickHouse connection
CLICKHOUSE_URL=http://host.docker.internal:8123
